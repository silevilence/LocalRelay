# Change Log

## V0.1.1

### ✨ 新功能

- **应用内自动更新**：支持在应用内检查 GitHub 最新稳定 Release，并展示新版本号、发布时间与更新说明
- **安装范围自动匹配**：根据当前 LocalRelay 是用户级安装还是机器级安装，自动下载对应的 NSIS 安装包
- **更新下载与校验**：下载安装包时展示进度，并使用 Release 中的 `checksums.txt` 进行 SHA-256 完整性校验
- **静默覆盖安装与重启**：校验通过后退出应用，静默启动安装包覆盖原安装目录，并在安装完成后重新启动 LocalRelay
- **跳过版本与稍后处理**：更新提示支持稍后处理与跳过当前版本，避免重复打扰

### 🚀 优化

- **Release 构建注入版本信息**：打包时将 tag 版本号与发布仓库注入应用，确保自动更新检查的当前版本与 GitHub Release 对齐
- **发布流水线适配自动更新**：GitHub Actions 构建时注入当前仓库名，方便后续迁移或 fork 后继续使用自动更新

### 🐛 Bug 修复

- **覆盖非默认安装目录**：自动更新静默安装时传入当前安装目录，避免用户修改安装路径后更新到默认目录

## V0.1.0

### ✨ 新功能

- **首个可用版本发布**：本地运行的大模型中转网关桌面软件，打开即用，无需 Docker 或额外运行时环境
- **供应商与模型管理**：可在界面中统一添加、编辑、删除多个大模型供应商及其模型配置，内置 DeepSeek、火山引擎、硅基流动等常用供应商预设，一键添加
- **预设模板快速接入**：内置常用供应商预设模板，添加供应商时可通过可筛选浮窗快速选择，自动填充默认地址与能力配置
- **统一模型路由**：对外以「供应商/模型」形式统一路由，可自由设置对外提供的模型范围
- **多协议入站支持**：支持以 OpenAI Chat Completions 协议接入，兼容流式与非流式两种调用方式
- **多协议出站适配**：对上游同时支持 OpenAI Chat、Anthropic Messages、Google Gemini、OpenAI Responses 四种协议，按各供应商实际接口自动转换
- **流式响应转发**：完整支持流式输出链路，工具调用与思考内容在流式场景下可正确传递
- **供应商能力差异兼容**：通过可配置的能力描述层处理不同供应商的差异字段（如思考开关、思考力度等），新增供应商无需改动核心逻辑
- **连通性测试**：添加供应商或模型后可在界面中直接发起测试调用，验证配置是否正确
- **网关访问密钥管理**：可生成与管理用于访问本网关的密钥，支持命名、备注、复制与显示切换，删除采用软删除以保证历史可追溯
- **调用日志记录**：自动记录每次调用的输入输出、耗时、状态码、协议类型等信息
- **Token 用量统计**：按时间区间、供应商、模型、应用维度统计 Token 用量，区分输入、输出与缓存命中的 Token
- **统计图表可视化**：基于 ECharts 提供饼图、柱形图、折线图等多种可视化图表，支持按小时、按天、按周统计
- **请求日志表格**：可分页查看调用明细，支持导出 CSV，并按供应商、模型、应用维度查看聚合统计
- **Windows 安装包**：提供 NSIS 打包的 Windows x64 安装包，支持安装、卸载、开始菜单快捷方式与静默安装
