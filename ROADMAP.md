# 项目开发路线图 (Roadmap)

## 📅 计划中

- [ ] **完善错误处理与统一异常响应**
  - [ ] 设计跨协议统一的错误码/错误结构
  - [ ] 实现各协议出入站的错误格式转换
  - [ ] 补充关键路径的异常场景日志记录

- [ ] **界面体验打磨**
  - [ ] 完善操作引导（首次使用引导、空状态提示）
  - [ ] 统一视觉风格与交互细节
  - [ ] 补充关键操作的二次确认与反馈提示

- [ ] **编写用户文档与使用说明**
  - [ ] 编写快速上手指南（安装、添加供应商、发起首次调用）
  - [ ] 编写供应商配置与预设说明文档
  - [ ] 编写常见问题与故障排查文档

## 🚧 开发中

- [ ] **新增特殊提供商类型聚合**
  - [ ] 概念：聚合 Provider（类型 `aggregation`，无真实 BaseURL/APIKey，仅作容器）下挂聚合模型；聚合模型配置有序成员列表（引用 `providerId/modelId`）+ 转发策略；成员必须是真实模型，禁止嵌套（成员不能是聚合模型）
  - [ ] 数据模型：新增 `aggregation_configs` 表（`model_id` 主键 → JSON 配置）；聚合模型 `context_length`/`max_tokens` 存 0、`capabilities` 存空串，创建时强制校验聚合配置合法（至少 1 个有效成员）；聚合 Provider 表单隐藏 BaseURL/APIKey，列表页显示"聚合"徽标，其 `capability_config` 不参与协议解析
  - [ ] 转发策略-主备：成员列表第一项为主、其余按序为备；失败判定：非流式（连接失败/超时/4xx/5xx/解析失败）均切换；流式仅首个 SSE 事件前切换；熔断：失败成员进入冷却期（默认 60s 可配置，内存态重启清零），期间跳过、到期自动重试；成员尝试超时默认 10s 可配置（普通请求不变）；客户端断开即中止切换循环；已知行为：切换可能产生重复请求（at-least-once），文档注明
  - [ ] 转发策略-轮询：按聚合模型各自游标循环分配，失败不重试（容灾是主备的职责）
  - [ ] 转发策略-按 Token 均衡：选最近 1 小时滑动窗口内累计消耗最少者（口径 `input+output+cache_creation`，排除 cache_read）；内存自维护计数器，请求完成后更新，不做在途计数（近似即可）
  - [ ] 转发策略-分时：例外表形式（用户配置若干"小时 0-23 → 成员"行），未配置时段回退到成员列表第一项，失败不重试
  - [ ] 成员失效（被删/禁用）运行时静默跳过，全部失效报聚合错误；删除被聚合引用的模型时提示确认但不阻止
  - [ ] 错误语义：全部成员失败返回聚合层包装错误（含按成员顺序的失败摘要）；成功请求响应 `model` 字段如实透传实际成员 id；不设整体超时上限（客户端自设）
  - [ ] 能力交集（动态计算，仅展示不校验）：`contextLength`/`maxTokens` = 成员最小值；`capabilities` 各能力键逻辑与；流式不做自动降级；请求参数原样转发，不做聚合级参数覆盖
  - [ ] 请求链路：聚合层只做"选成员"——`GetRoutedModel` 解析出聚合模型 → 实时读 DB 查配置（每次请求读、配置即时生效、请求开始时快照）→ 按策略选成员 → 以成员 Provider 的 `capability_config` 交还现有链路；普通模型路径零改动
  - [ ] 统计与日志：`call_logs` 新增 `aggregation_source`（经由的聚合模型）与 `agg_attempts`（失败明细 JSON）两列（走 schema 迁移）；日志记实际成员一条，统计数据自动归入基础模型；统计页聚合维度可见（token 不重复计），筛选器含聚合模型（按 `aggregation_source` 匹配）
  - [ ] 前端界面：聚合模型编辑弹窗（与普通模型表单互斥）含成员有序列表（两级下拉、排除聚合模型、支持上下移/删除）、策略参数表单（主备冷却期/按Token/分时例外表）、能力交集只读预览（成员变更实时刷新）
  - [ ] 并发与测试：每聚合模型一把锁（`sync.Map` 存锁实例），熔断/计数器/游标受锁保护；单元测试覆盖策略选择/熔断/交集/错误包装/分时边界（熔断器时钟注入 `now` 函数）；集成测试用 httptest mock 上游验证主备 HTTP 层切换与流式首事件前切换；新模块（策略选择器、熔断器、交集计算器）独立成文件；提交前用真实 API Key 手动验证一次主备调用

## ✅ 已完成

- [x] **初始化项目目录与本地 Git 仓库**
  - [x] 初始化 Go module，确定模块名与目录结构（cmd/、internal/、frontend/、docs/ 等）
  - [x] 初始化本地 Git 仓库并创建 `.gitignore`
    - 忽略构建产物、node_modules、本地数据库文件、IDE 配置
  - [x] 提交初始 commit，包含 AGENTS.md、ROADMAP.md 与空目录结构

- [x] **搭建 Wails 项目骨架**
  - [x] 使用 Wails 脚手架生成基础项目（选定 v2）
  - [x] 验证前端（React + Tailwind）与 Go 后端能正常联调启动
  - [x] 确认 Windows 下本地构建可产出可运行 exe

- [x] **设计并落地 SQLite 数据库 Schema（第一版）**
  - [x] 设计 provider 表结构（id、baseUrl、类型、apiKey、名称等）
  - [x] 设计 model 表结构（id、provider关联、能力字段、上下文长度、max_tokens等）
  - [x] 设计调用日志表结构（用于后续 Token 统计）
    - 预留输入/输出/缓存 token 字段
  - [x] 集成纯 Go SQLite 驱动，编写数据库初始化与迁移逻辑

- [x] **实现 Provider/Model 管理后端接口**
  - [x] 实现 provider 的增删改查接口
  - [x] 实现 model 的增删改查接口（关联所属 provider）
  - [x] 编写基础单元测试覆盖 CRUD 逻辑

- [x] **实现 Provider/Model 管理前端界面**
  - [x] 实现 provider 列表与新增/编辑表单
  - [x] 实现 model 列表与新增/编辑表单
  - [x] 完成基础的表单校验与操作反馈提示

- [x] **设计内部统一协议格式（IR，非流式）**
  - [x] 定义内部消息结构（文本、多模态、工具调用、思考内容的表达方式）
  - [x] 编写 IR 数据结构对应的 Go struct 与文档说明
  - [x] 编写至少 3 个手工示例（覆盖纯文本、工具调用、多模态）验证 IR 表达能力

- [x] **实现 OpenAI Chat → IR 的入站解析**
  - [x] 解析标准 OpenAI Chat 请求体（messages、tools、参数等）映射为 IR
  - [x] 处理 system 消息、多轮对话、工具调用请求的映射
  - [x] 编写单元测试覆盖典型请求样例

- [x] **实现 IR → 常用供应商真实请求的出站映射（手写，先支持1~2家）**
  - [x] 选定优先支持的供应商（如 OpenAI 官方 + DeepSeek）
  - [x] 编写 IR 到该供应商真实请求体的映射逻辑
  - [x] 编写单元测试验证映射正确性

- [x] **打通端到端非流式调用链路**
  - [x] 实现 HTTP 路由：接收 OpenAI Chat 请求 → IR → 出站请求 → 得到响应
  - [x] 实现供应商响应 → IR → OpenAI Chat 响应格式的回程转换
  - [x] 用真实 API Key 完成至少一次端到端手动验证

- [x] **实现统一模型标识路由（providerId/modelId）**
  - [x] 实现请求中模型标识解析与匹配逻辑
  - [x] 实现"可设置对外提供哪些模型"的开关与过滤逻辑
  - [x] 处理模型不存在/未启用等异常情况的错误响应

- [x] **实现调用日志记录**
  - [x] 在请求链路中埋点记录每次调用的输入输出、耗时、状态
  - [x] 记录 token 使用数据（输入/输出，若支持则含缓存 token）
  - [x] 编写日志写入的异步/批量处理，避免影响请求性能

- [x] **实现 Token 统计后端接口**
  - [x] 实现按时间区间统计输入/输出 token 的聚合查询接口
  - [x] 实现按供应商/模型维度筛选统计
  - [x] 实现缓存 token 的单独统计逻辑（供应商支持时）

- [x] **实现 Token 统计前端界面**
  - [x] 实现统计图表（按区间展示输入/输出 token 趋势）
  - [x] 实现筛选交互（时间区间、供应商、模型）
  - [x] 实现缓存/非缓存 token 的区分展示

- [x] **设计供应商能力配置表结构（差异兼容层）**
  - [x] 定义思考开关、思考内容回传、reasoning_effort 等差异字段的配置schema
  - [x] 将已支持的供应商差异逐项迁移为配置数据，替换手写 if-else
  - [x] 编写配置驱动的映射逻辑单元测试

- [x] **实现预设模板功能**
  - [x] 设计预设模板数据结构（默认 baseUrl、能力配置等）
  - [x] 实现前端"从预设直接添加供应商"的交互
  - [x] 内置至少 2~3 个常用供应商预设（DeepSeek、火山引擎CodingPlan、Opencode GO、硅基流动）

- [x] **UI优化**
  - [x] **供应商预设选择改为可筛选浮窗**：添加供应商时的预设卡片列表移入浮窗弹出，支持筛选搜索，浮窗内展示更多预设信息
  - [x] **修复 API 密钥显示/隐藏按钮图标**：当前眼睛按钮误显示为"X"，改为正确的显示/隐藏切换图标
  - [x] **清理页面上意义不明的符号**：排查并替换页面中出现的含义不明图标（如 API 密钥和 API 地址旁的未知符号），替换为语义清晰的图标或移除
  - [x] **供应商能力配置改为用户友好界面**：将"差异兼容配置 JSON"重命名为"提供商属性配置"，用结构化表单替代原始 JSON 输入框，提供最终 JSON 配置的语法高亮预览，默认折叠、需要时展开
  - [x] **模型能力配置改为用户友好界面**：将模型的"能力 JSON"输入框改为结构化表单 UI，不再让用户直接手写 JSON
  - [x] **校正模型预设参数**：核查所有预设模型的上下文长度和 Max Tokens 等默认值是否正确，修正错误数据
  - [x] **Token 统计与模型配置页面分离**：将 Token 统计功能从模型配置页面移出，放到独立的统计页面
  - [x] **模型详情配置改为弹窗**：模型的编辑/详情操作改为弹出浮窗完成，不在主页面上切换视图

- [x] **实现流式响应内部事件格式设计**
  - [x] 参考 Anthropic 事件流语义设计内部流式事件类型
  - [x] 编写流式 IR 事件的 Go 数据结构
  - [x] 编写文档说明各事件类型的语义与顺序约束

- [x] **实现 OpenAI Chat 流式请求的入站与出站转换**
  - [x] 实现 OpenAI Chat SSE 请求解析为内部流式事件
  - [x] 实现供应商流式响应转换为内部流式事件
  - [x] 实现内部流式事件转换回 OpenAI Chat SSE 格式返回客户端

- [x] **端到端验证流式调用链路**
  - [x] 使用真实 API Key 完成流式调用手动验证
  - [x] 验证工具调用、思考内容在流式场景下的正确传递
  - [x] 处理流式中断、超时、异常断开等边界情况

- [x] **API Key功能与统计提升**
  - [x] 前置依赖
    - [x] 引入 ECharts 作为前端图表库（用于饼图、柱形图、折线图），替换当前手写 SVG 趋势图
    - [x] 实现 Schema 版本化迁移机制：利用已有 `schema_migrations` 表，支持按版本号顺序执行 ALTER TABLE 增量迁移，后续新增字段走迁移流程而非依赖 CREATE TABLE IF NOT EXISTS
  - [x] 添加一个与配置和Token统计平级的界面：Api Key管理
    - [x] 可以管理访问此网关要使用的Api Key（增删改查）
    - [x] 每个apikey有名称、描述、key三个属性，其中key为软件自动生成（格式为 `sk-` + 32位字符，直接取 GUID 去横线生成），名称与描述由用户输入
    - [x] 一个应用对应一个key（不支持一个名称关联多个key），从简实现
    - [x] 不需要保密处理，只用于基本的统计口径；不需要启用/禁用等鉴权相关功能
    - [x] 名称允许重复，但"无应用"为保留显示名，禁止用户使用
    - [x] 删除采用软删除（标记删除而非物理删除），以保证历史日志可追溯
    - [x] UI 交互：key 支持复制按钮、显示/隐藏切换（长 key 截断显示）
  - [x] apikey识别与统计关联（一律放行，不拒绝请求，仅影响统计列）
    - [x] 增加对apikey的识别（按OpenAI Chat接口的要求来传输：`Authorization: Bearer sk-xxx`）
    - [x] 统计中增加一个应用列；call_logs 冗余存储 app_name 快照（B方案），key 改名或软删除后历史日志仍显示原快照名称
    - [x] 当传输有效（存在且未软删除）的api key时，统计中应用列设置为此key对应的名称
    - [x] 当传输无效的api key或没有传输api key时，统计中应用列设置为"无应用"
    - [x] 统计查询增加按应用名称的筛选
  - [x] 统计增强
    - [x] 增加统计表格区（同时只显示一种表格，类tab页形式切换）
      - [x] 请求日志表格（调用时间、提供商、模型、应用、输入、输出、缓存命中、状态码、协议、耗时ms、是否流式、错误信息）；支持分页（每页50-100条）；支持导出CSV
      - [x] 按提供商统计表格（名称、调用次数、输入、输出、缓存写入、缓存命中、总Token、占比）
      - [x] 按模型统计表格（同上列结构）
      - [x] 按应用统计表格（同上列结构）
    - [x] 增加统计图表区（同时只显示一个统计图，类tab页形式切换）（各折线/柱形图均可选择按小时、按天、按周统计；下限：按小时无下限，按天要求时间范围 ≥1 天，按周要求时间范围 ≥7 天）
      - [x] 不同应用消耗Token饼图
      - [x] 不同模型消耗Token饼图
      - [x] 不同提供商消耗Token饼图
      - [x] 按时间Token消耗柱形图对比（柱为总消耗Token数；可选按模型、按应用显示堆积柱形图）
      - [x] Token消耗趋势图（折线图）（可切换：输入、输出、缓存命中三条线；每个模型一条线；每个应用一条线）

- [x] **扩展支持更多供应商适配（按个人常用列表逐个补充）**
  - [x] 逐个供应商编写配置与必要的定制映射逻辑
  - [x] 逐个供应商完成手动调用验证
  - [x] 补充对应的预设模板

- [x] **实现 IR → Anthropic Messages 协议的出站转换（对外接口）**
  - [x] 实现非流式响应转换
  - [x] 实现流式响应转换
  - [x] 编写测试验证与官方 Anthropic 客户端的兼容性

- [x] **实现 IR → Google Gemini 协议的出站转换（对外接口）**
  - [x] 实现非流式响应转换
  - [x] 实现流式响应转换
  - [x] 编写测试验证与官方 Gemini 客户端的兼容性

- [x] **实现 IR → OpenAI Response 协议的出站转换（对外接口）**
  - [x] 实现非流式响应转换
  - [x] 实现流式响应转换
  - [x] 编写测试验证兼容性

- [x] **本地打包安装包并通过测试**
  - [x] 配置 Wails NSIS 打包参数，构建 Windows x64 安装包。
  - [x] 验证安装包支持安装、覆盖安装、卸载、开始菜单快捷方式。
  - [x] 验证静默安装参数可用，为后续自动更新覆盖安装做准备。
  - [x] 本地完整跑通打包流程，产出可正常使用的安装包。

- [x] **搭建 GitHub Actions 自动构建与发布流水线**
  - [x] 定义发布规范：每个 Release 包含 NSIS 安装包、`checksums.txt` 与版本描述；统一使用语义化版本号。
  - [x] 以 `changelog.md` 作为 Release 描述来源，workflow 按触发 tag 的版本号解析对应章节作为 Release body：
    - 文件格式：首行 `# Change Log`；每个版本以 `## V<版本号>` 为二级标题（大小写不敏感，兼容 `v` 与 `V` 前缀），标题下按需包含 `### ✨ 新功能` / `### 🚀 优化` / `### 🐛 Bug 修复` 三个三级标题，每类下为 `- ` 开头的条目列表。
    - 解析规则：从触发的 tag 提取版本号，在 `changelog.md` 中匹配 `## V<版本号>` 二级标题（大小写不敏感），取该标题之后到下一个 `## ` 标题或文件末尾之间的内容作为 Release body。
    - 容错：若 `changelog.md` 不存在或未匹配到对应版本章节，回退使用 tag 名称作为 Release body 并在日志中告警。
  - [x] 配置 GitHub Actions workflow，通过推送 `v*.*.*` 格式的 tag（大小写不敏感，兼容 `v` 与 `V` 前缀）触发自动构建。
  - [x] 构建产物自动生成 `checksums.txt`（SHA-256）并上传至对应的 GitHub Release，并以 `changelog.md` 中该版本章节内容作为 Release body。
  - [x] 验证从打 tag 到产出可下载安装包的完整流程。

- [x] **实现应用内自动更新功能**
  - [x] 实现 GitHub Release 更新检查，获取最新稳定版本并与当前版本比较，支持忽略预发布版本。
  - [x] 实现前端更新提示界面，展示新版本号、发布日期、更新说明，并提供下载、稍后处理与跳过此版本操作。
  - [x] 实现 NSIS 安装包下载与 SHA-256 完整性校验，下载过程展示进度、失败原因和重试入口。
  - [x] 下载完成后退出应用并以静默参数启动 NSIS 安装包，覆盖安装后自动重新启动 LocalRelay。
  - [x] 增加更新异常处理与日志记录，确保网络失败、校验失败、安装器启动失败时不影响当前应用正常使用。

- [x] **系统功能增强**
  - [x] 以下功能以 Windows 为准实现；其它系统上不支持的需做好兼容（至少不崩溃），完整跨平台适配留待后续任务
  - [x] **应用图标**
    - [x] 源图 `build/appicon.png` 已就位，作为最终定稿图标（尺寸/透明度已确认）
    - [x] 本次仅处理 Windows：从 `build/appicon.png` 生成多尺寸 ICO（含 256/128/64/48/32/16 多分辨率），覆盖 `build/windows/icon.ico`
    - [x] 同步更新 `wails.json` 中 icon 相关字段（若有引用）
    - [x] macOS（`.icns`）/Linux 留待后续兼容任务，本次不处理
  - [x] **系统托盘图标**
    - [x] 引入 `github.com/energye/systray`（纯 Go 无 CGO，跨平台）实现托盘
    - [x] 只要应用在运行，托盘图标始终显示（不受下方两个开关影响）
    - [x] 托盘图标复用 `build/appicon.png` 转出的 ICO
    - [x] 双击托盘图标 → 显示主窗口
    - [x] 右键菜单项：`显示主窗口` / `暂停网关服务`|`恢复网关服务`（文字随网关开关状态切换，与设置页开关双向同步）/ `退出`（始终为真正退出）
    - [x] 设置页新增两个独立开关（均默认开启）：
      - [x] `最小化时隐藏到系统托盘`：开启时点窗口最小化按钮改为隐藏窗口到托盘
      - [x] `关闭时隐藏到系统托盘`：开启时点窗口 × 按钮改为隐藏窗口到托盘而非退出；首次隐藏时弹一次性提示气泡（"已隐藏到系统托盘，可从托盘图标右键退出"），设置页该开关旁注明此说明
  - [x] **开机启动**
    - [x] 通过注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 实现（用户级、无需提权）
    - [x] 设置页新增开关 `开机启动`：开启时写入注册表项（值为 `"<exe路径>"`），关闭时删除该项
    - [x] 设置页新增独立开关 `启动时自动缩小到托盘`（默认开启，不依赖开机启动开关，可单独设置）：开启时每次应用启动均根据本地设置直接隐藏主窗口到托盘，关闭时正常显示窗口
    - [x] 启动隐藏不依赖命令行参数，也不依赖"最小化时隐藏到托盘"开关（启动态与运行态语义分离）；托盘图标照常显示，用户可从托盘点开主窗口
  - [x] **网关服务开关**
    - [x] 设置页端口设置附近新增服务开关，默认开启（即当前行为）
    - [x] 开关状态持久化到 SQLite（与 `RelayPort` 同机制），应用启动时读取：开启则启动网关，关闭则跳过
    - [x] 关闭时调用 `http.Server.Shutdown`（3 秒超时，等待在途请求），不销毁 `relay` 实例以便重新开启
    - [x] 开启时重新 `listenRelay` + `serveRelay`
    - [x] 服务关闭时端口设置仍可编辑保存到 DB，但不立即重启监听；下次开启服务时按新端口启动
    - [x] 托盘菜单"暂停/恢复网关"与此开关双向同步：托盘点击翻转开关并同步前端 UI，前端翻转也同步更新托盘菜单项文字
  - [x] **本机访问地址列出**
    - [x] 设置页端口设置下方新增"本机访问地址"区块
    - [x] 列出以下地址（每条一行，形式 `http://<ip>:<port>`，端口取当前 `RelayPort`）：
      - [x] `http://127.0.0.1:<port>`（本地回环，始终列出）
      - [x] `http://<各网卡 IPv4>:<port>`（遍历 `net.Interfaces` 取非 loopback 的 IPv4，仅 IPv4 不列 IPv6）
      - [x] `http://<主机名>:<port>`（`os.Hostname()`，方便局域网用主机名访问）
    - [x] 不主动过滤虚拟网卡（Docker/VMware/VPN 等），全部列出；每条地址后标注来源网卡名（如 `192.168.1.10 (以太网)`），跳过 `0.0.0.0` 与 `127.0.0.1` 重复
    - [x] 刷新时机：设置页打开时刷新一次、端口变更保存后刷新、提供手动"刷新"按钮；不做实时网卡监听
    - [x] 每条地址后一个复制按钮，复制完整 `http://<ip>:<port>` 到剪贴板，复用现有 API Key 复制按钮交互样式（图标 + 复制成功反馈）；不提供"一键复制全部"

- [x] **实现对外协议入站解析补全（Anthropic/Gemini/OpenAI Response → IR）**
  - [x] 实现 Anthropic Messages 请求解析为 IR
  - [x] 实现 Gemini 请求解析为 IR
  - [x] 实现 OpenAI Response 请求解析为 IR
  - [x] 各协议入站时按各自方式识别 API Key 并写入统计应用列（Anthropic: `x-api-key`；Gemini: `x-goog-api-key` 或 query `key`；OpenAI Response: `Authorization: Bearer`），识别规则与 OpenAI Chat 入站保持一致
