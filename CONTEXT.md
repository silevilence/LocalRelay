# LocalRelay Context

本地 LLM 中转网关的领域词汇表。网关将请求按 `providerId/modelId` 路由到各上游提供商，并支持以聚合模型作为多成员转发的入口。

## Language

### 聚合结构

**聚合 Provider（Aggregation Provider）**：
一种特殊类型的 Provider，不对应真实上游服务，仅作为聚合模型的容器。
_Avoid_: 虚拟提供商、转发提供商

**聚合模型（Aggregation Model）**：
聚合 Provider 下的模型，对外以自身 id 暴露，实际将请求转发给配置的成员模型，并按配置的策略选择具体成员。
_Avoid_: 虚拟模型、别名模型

**成员（Member）**：
聚合模型引用的真实模型（真实 Provider 下的模型）。成员不能是另一个聚合模型。
_Avoid_: 子模型、目标模型、后端模型

### 转发策略

**主备模式（Primary-Backup）**：
一种转发策略：成员列表第一项为主模型，其余按顺序为备选；主模型请求失败时按序切换到备选，直到成功或全部失败。
_Avoid_: 故障转移、failover

**冷却期（Cooldown）**：
主备模式下，请求失败的成员在一段时间内被跳过、不再参与选择的机制；冷却期结束后该成员重新参与选择。
_Avoid_: 熔断、断路器（circuit breaker 是通用术语，本项目的"跳过失败成员一段时间"语义用冷却期表达）

**轮询（Round-robin）**：
一种转发策略：请求按成员顺序循环分配；分配到某成员的请求失败时不尝试其他成员。
_Avoid_: 循环调度

**按 Token 均衡（Token-based Balancing）**：
一种转发策略：将请求分配给最近统计窗口内累计消耗 Token 最少的成员。

**分时使用（Time-slot Schedule）**：
一种转发策略：由用户配置"小时 → 成员"的时段表，请求按当前小时选择成员；未配置的时段使用默认成员。
_Avoid_: 定时切换、时段策略

### 统计

**聚合来源（Aggregation Source）**：
一条调用记录经由的聚合模型。聚合调用的统计口径归属实际成员，聚合来源仅用于追溯"这次调用是通过哪个聚合模型产生的"。
_Avoid_: 备注、来源模型
